const Form = [
    {
        "page" : "FAQ",
        "section" : "form",
        "type": "email",
        "regex": /^([a-z\d\.-]+)@([a-z\d-]+)\.([a-z]{2,8})(\.[a-z]{2,8})?$/
    }
]

export default Form